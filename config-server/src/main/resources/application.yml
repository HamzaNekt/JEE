spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URI:https://github.com/HamzaNekt/immo-config-repo}
          default-label: main
server:
  port: ${PORT:8888}

management:
  endpoints:
    web:
      exposure:
        include: health,info,env

